{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Native C++ (LLDB)",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/main",
      "cwd": "${workspaceFolder}",
      "stopOnEntry": false,
      "preLaunchTask": "Emscripten: Build (Debug)",
      "console": "integratedTerminal"
    }
  ]
}
